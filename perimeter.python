class Rectangle:
    def __init__(self, length, width):
        self.length = length
        self.width = width

    def perimeter(self):
        result = 2 * (self.length + self.width)
        print("Perimeter:", result)


r = Rectangle(10, 5)
r.perimeter()
